import { clr } from "../constants/colors";

export const nav: Record<string, string> = {
    link: `text-sm font-normal text-neutral-50 hover:text-slate-300 bg-transparent hover:bg-[#212733] hover:text-white rounded-sm px-3 py-1 duration-300`
}